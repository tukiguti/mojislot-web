# MOJISLOT TODO

2026-07-15 の実装・設計・関連リポジトリ調査で見つかった、即時修正の範囲を超える課題。
今後の制御・演出改善を以下で継続管理する。完了項目には実装時の決定を残す。

## P0: 仕様を決めてから着手

- [x] レバー時の内部役抽選を導入し、演出抽選と当選結果を分離する（2026-07-16）
  - `miss / replay / core / cherry / reg / big` などの内部役を `RoundContext` に保持する
  - 各章の具体役ごとに`internalRoleRate`を持たせ、種別やリール文字枚数を介さず直接抽選する
  - 「ガセ演出なし」を維持し、当選役を表現できる演出だけをレバーON後に抽選する
  - 表示役は`RoleResolver`で内部役と一致するラインだけを成立扱いにする
  - 初期レートは暫定値。再現可能シミュレーター完成後に再調整する
- [x] レバー時の内部役抽選を1枚役へ拡張（2026-07-24）
  - 表示役と内部役を分離（表示役8＋1枚役 → 内部役10）
- [x] 押し順役・押し順ナビを廃止（2026-07-25）
  - 押し順は「役の種類」ではなく**停止制御の入力**という設計へ戻した。順押しが前提
  - 内部役 13種 → 10種。だんご・みかんは普通の小役になり、示唆/クイズ/狙えにも登場する
- [x] 1枚役を実役化（2026-07-25）
  - 「2個テンパイ＝1枚」の擬似実装を廃し、各章2〜3種の**実在の並び**（既存文字の
    「単語にならない」3文字・`singleYaku`）を定義。判定・払い出し・蹴りの一般レイヤに載せた
  - 選定は「miss時に蹴りで必ず回避できる（②漏れ0）」を制約に引き込みカバー率を最大化
    （food なしん/みしご/もしん 80% ・ verb 81% ・ animal 76% ・ security 90% ・ yasai 80%）
  - 当選時は自動引き込み（中段・窓4）＝実機の「当選小役は勝手に揃う」。窓外はこぼれ
  - コンボ・図鑑・成立演出には乗せない（こぼし扱い）。フラグは複数ID（exceptYakuIds）へ一般化
  - `replay` kind は廃止（再遊技は実装しない決定）。とうふ等は core へ整理
- [x] 停止制御を決定的なテーブル制御へ移行（2026-07-24）
  - 当選役以外を4コマ以内で必ず回避。確率・演出限定・カテゴリ限定を撤廃
  - リール配列を構築的に再設計し②（揃っているのに0枚）を全5章ゼロ化（tests/audit）
- [x] 本番ロジックを利用する再現可能な出玉シミュレーターを作る（2026-07-24）
  - `tests/sim/payout-sim.test.ts`（`SIM=1` で実行）。腕別の機械割を再測定しレート再調整済み
- [x] ビタ押しを出玉へ反映する（2026-07-27）
  - 判定を「**引き込みなし＝自力停止**」へ変更。役に必要なリールを1本残らず
    自力で止めた時だけ配当に上乗せする（`payout.bitaMultiplier`）
  - 従来の ±12ms は出目に一切影響しない指標だった（どちらにせよ最寄りセルへ
    スナップされる）ので、押し味のSE・波紋にだけ残しスコアからは外した
  - 貢献リールを役の文字数で数えるよう修正。チェリー（2文字役）で右リールの
    自力停止まで要求してしまっていた
  - 到達率（実測）: 初心者4.1% / 中級10.0% / 上級27.1% / 神69.6%
- [x] フリーズを「強レア役」化する（2026-07-26）
  - 独立抽選をやめ、内部役テーブルに `freeze: true` のBIG専用役（各章1/2000・ボーナス中0）を追加。
    確率の一次情報は `data/yaku/` へ一本化し、`tuning.freeze` は `spinSpeed` だけになった
  - `forYaku` はフリーズ役を選ばない＝確定告知ランプ・持ち越しの消化で暴発しない
  - 出玉シミュレーションが 80.1/103.5/130.1/170.3% → 81.7/105.6/132.5/173.4% に変化したが、
    これは**測定の修正**。従来フリーズはテーブル外だったためシミュレーターが数えておらず、
    実際の機械割は前後でほぼ不変（差は1e-6オーダー）

## P0: 仕様を決めてから着手（続き）

- [x] 台選びをホールの島形式にし、設定（1〜6）と設定推測を導入（2026-07-27）
  - 6島×4台＝24台。同じ島はリール配列が同じで違うのは設定だけ＝覚えた配列が島の中で活きる
  - 設定は日付＋台番号から決定的に決まる（打っている間は変わらない／台ごとに違う／日替わり）
  - 小役では差が付かない（数百ゲームで見抜けると推測が成立しない）
  - ホール方針（末尾／島／角／全台系／通常）→ 各台の設定 → ポスターの順で決める
  - 設定示唆はボーナス終了画面。「出たら意味がある／出なくても否定にならない」
  - 詳細: zikken/playground/mojislot-plan/30_hall-and-settings.md
- [x] 役・島構成を作り直す（2026-07-27）
  - 役名とリールに出る文字が食い違う（ナマズ／なまず、ドラゴン→ドラゴ）、ジャンルから
    浮いた役（ポテチは野菜でない、メモリ・コールはセキュリティ用語でない）を一掃した
  - REGはリールに自分の文字を持たない（BIG1の頭2文字＋別の役の右）。この制約から
    **BIG2つとREGの3語が同時に成立する組**を探す形に変えた。「寿司酢」は文字の余りに
    名前を付けたものだった
  - カタカナの野菜名は2文字目で分岐しないため野菜だけではREGを作れず、果物まで広げて
    ライチ／ライム／プラムに。島名を**カタカナ八百屋**へ（内部IDは `yasai` のまま）
  - ②はリール配列の焼きなましで再度ゼロ化。探索ツールを `tests/tools/optimize-reels.test.ts`
    としてリポジトリに残した（前回はスクリプトが失われ、手法から書き直しになった）
  - 1枚役も文字が変わるので組み直した。②を抑えようとコマ数を絞ると引き込みが届かなくなる
    ので、3文字のコマ数の積が36〜48に収まる語を選び成立率43〜55%に揃えた（一度10%まで
    落として出玉を下げた）。増えた②は焼きなましで処理する、という順序に落ち着いた
  - 出玉は 初心者89.1% / 中級110.2% / 上級140.1% / 神196.2%（作り直し前より3〜6ポイント高い。
    ボーナスの狙いやすさが上がったため）
  - 詳細: zikken/playground/mojislot-plan/28_yaku-chapter-redesign.md
- [x] 役の到達性監査を追加（2026-07-27）
  - `tests/audit/yaku-reachability.test.ts`。②の監査は「当選していない役が出ないか」しか
    見ておらず、**当選しているのに絶対に揃わない**を検出できなかった
  - 実際に八百屋のREGが成立率0%になり、永久持ち越し→無告知→島が丸ごと機能停止していた。
    全テストが通ったまま壊れ、出玉シミュレーションの数字で初めて気づいた
- [x] カットインを役色から手続き生成する（2026-07-28）
  - 一枚絵を**任意**にした。役データに `cutinArt` を書いた役だけが絵を使い、書かない役は
    役色から組み立てる（放射グロー・光線・縁が役色、背景に役名を輪郭で沈める）
  - 以前は「主役＝島の一枚絵、バー役＝`_bar` 絵」と役の位置で暗黙に決めていたため、役を
    差し替えると絵だけ前の役のまま残った。役が自分の絵を名指しする形にして構造的に解消
  - 副産物として BIG1=金 / BIG2=青 / REG=銀 と色が分かれた（以前はBIG2も金だった）
- [ ] カットインの一枚絵を用意するか決める（**画像生成が要る**）
  - 今は全島とも生成側で出ている。絵を足すなら役へ `cutinArt` を1行書く
  - 動詞島の2枚（いきる＝命の樹／やすむ＝三日月）は役と一致しているのですぐ使える
  - 対応表: zikken/playground/mojislot-plan/16_art-assets-and-lcd-layout.md
- [ ] 台選び画面をホール島形式へ差し替える（**デザイン待ち**）
  - 裏側（24台・方針・ポスター・設定・データ・示唆）は実装済み。画面だけ未配線
  - 受け渡し資料: zikken/playground/mojislot-plan/29_design-handoff.md
- [x] リール図柄は色タイル＋文字を正式とする（2026-07-28・判断のみ、コード変更なし）
  - 画像トグルは既定OFFなので普段誰も見ていない。ビタ押しが出玉に直結する今、図柄の
    識別しやすさは難易度そのもので、文字が読みやすい方が競技性に合う
  - 「絵と文字の融合（カリグラム）」案は見分けに一拍かかる＝難易度を上げる方向だったので見送り
  - `noArt` の仕組みで絵を用意した役だけ絵になる。決めたのは「今は作らない」だけ
- [x] 通常時に無演出（none）を入れる（2026-07-28）
  - `rollAvailable` は「役を表現できる演出」だけから抽選しており候補に none が無かった。
    そのため `effectRates.default.none = 0.5` はどこからも読まれず、当たれば必ず狙える状態
    だった。none を含む `roll()` も呼び出し元が無かったので削除した
  - 候補に none を混ぜた。ボーナス中は `rates.bonus.none = 0.0` なので重み0で落ちる
  - 演出が出るのは全ゲームの約28%（表示役を持つ内部役57% × 50%）。残りは何を狙うか
    分からないまま押すが、引き込みは当選役に効いたままなので当たりは拾える
- [x] 配当を組み替える（ボーナスの価値を「区間」へ寄せる／2026-07-28）
  - 小役 5枚一律 → **4 / 6 / 8 / 10**。枚数から役を逆算できるようにする（実機のベル・スイカ
    と同じで枚数が役の名札になる）。いちばん出やすい役をいちばん低配当に置いた
  - カテゴリ別の `baseMultiplier` では小役4種を区別できないので、**役ごとの `payout`** を
    追加した。リミックス島も「位置と枚数の対応」で覚える設計なので、枚数は役と同じ場所に置く
  - `tests/data/payout-tiers.test.ts` が全5島で階段の一致と「出やすい役ほど安い」を見張る
  - REG 14 → **10**、BIG 34 → **15**。恩恵は揃えた瞬間ではなくボーナス中の100%演出区間
- [ ] P2の演出設計（**役を明かす／明かさないの切り分け**が出玉設計そのもの）
  - 遅れ・無音・先告知/後告知・ブラックアウトは煽りで役を明かさない。これを既存枠に入れると
    狙える機会が減って出玉が下がり、追加枠にすると出玉は変わらない。どちらにするかを決める
  - 演出の種類が増えるだけでは機械割は動かない（当選は内部役テーブル、払い出しは配当で決まる）
- [ ] リミックス島のステージ切替を実装する（台の枠だけある状態）
  - **リール配列は共通、文字だけステージごとに変わる**。配列まで変わると覚え直しのコストが
    高すぎてステージ切替が苦行になる
  - **位置ごとの枚数をステージ間で共通にする**（位置3は常に6枚）。プレイヤーが覚えるのは
    「6枚の役はこの位置」という位置と枚数の対応になり、ステージが変わっても無効にならない
  - 制約: ②＝0 を5ステージ全部で満たす共通パターンが要る。焼きなましの目的関数を
    「5ステージの②の合計」に変えて探す。解が無ければステージごとに一部だけ差分を持たせる
  - 却下: 全ステージの文字を1本のリールに載せる案（35文字は21コマに入らない／ひらがなと
    カタカナの混在は識別の負荷が理不尽に寄る）
- [x] 出玉レートを再調整する（2026-07-28）
  - **初心者87.6% / 中級105.2% / 上級130.0% / 神176.0%**（変更前 89.1 / 110.2 / 140.1 / 196.2%）。
    設定1〜6で中級 101.3〜110.0%、突入 1/115〜1/93
  - 無演出を入れた時点で全腕が25点前後落ちて初心者64.6%になった。**水位は小役の枚数が
    ほぼ全部を決める**（1枚上げると全腕が10〜12点動く）ので、階段の間隔はそのまま2枚上げた。
    1枚刻みは微調整には粗すぎるので、数点を合わせる時はボーナス倍率の小数を使う
  - 無演出を薄くしても水位は戻らない（0.5→0.2 で初心者73.3%止まり、代わりに腕の開きが拡大）。
    狙える機会を増やすのは上手い人にだけ効くので、水位の調整弁にはならない
  - **腕の開きを詰められるのはコンボ天井だけ**。4.5→3.0 で神 −10点・初心者 −1点。
    2.5まで下げると開きはさらに詰まるが、ボーナス倍率2.2との差が0.3しか残らず消化中に
    コンボの伸びを感じられなくなるので3.0で止めた
  - 残る開きはボーナス中の目押し精度そのもの（純増 6.0枚 対 17.4枚）。ボーナス中は
    無演出が出ず狙う役は全員分かっているので、これ以上詰めるなら引き込みコマ数を
    増やすしかなく、それは目押しの価値を削る方向になる
  - 詳細: zikken/playground/mojislot-plan/31_effect-and-payout-plan.md §7
- [x] ボーナス中の1枚役をこぼしにする（2026-07-28）
  - ボーナス中も1枚役が5.7%で抽選されていた。表示役を持たないので演出が出ず、固定1枚で
    倍率も乗らず、しかも連チャンを切る。ボーナス中はハズレが0なので、上手い人にとって
    連を切る唯一の要因がこれだった。**BIGあたり11〜30枚**が消えていた
  - ボーナス中の抽選を0にして小役・チェリーへ比例配分（おかわり頻度は動かさないため
    REG/BIGには配らない）。代わりにボーナス中だけ許可リストへ1枚役を足し、当選役を
    引き込めなかった時の受け皿にした。通常時は従来どおり抽選で降ってくる役のまま
  - **引き込みの横取りに注意**。1枚役を許可リストへ入れると、図柄がたまたま近いという
    理由で本来取れる小役を捨てて1枚に落とす。カテゴリ優先だけでは足りず、当選役が
    引き込み圏外の時に蹴りで斜めに揃っていた目を潰していた（1章で59件）
  - 1枚役は引き込み対象から外し、**当選役が届かない最終停止でだけ**拾いに行く構造にした。
    あわせて pullIn の返り値を number|null にして「そのまま揃う(0)」と「届かない」を区別
  - `tests/audit/bonus-spill.test.ts` が全押下位置(21^3)で通常時とボーナス中を回し、
    通常時に揃う局面がボーナス中も必ず揃うことを見張る
  - こぼし率（ボーナス中に外したゲームのうち1枚役へ落ちた割合）: 初心者8.6% / 神5.1%
  - 上がった分はボーナス倍率 ×2.2→×1.95 で戻した。副作用でコンボの伸び幅が
    +36%→+54% に広がり、消化中にコンボが効く感触はむしろ強くなった
- [ ] 小役の枚数を図鑑に表示するか決める
  - 枚数を役の名札にした以上、打って覚える以外に確認手段が無い状態でよいか
  - 出すなら `ZukanOverlay` の行に1列足すだけ。ビタ押し前提のゲームで「配当表を見れば
    分かる」のが親切か、覚えること自体を遊びに残すかの判断

## P1: 制御と保守性

- [x] 予告・確定告知の8コマ強アシストを廃止し、実機準拠の一律4コマへ（2026-07-25）
- [x] ボーナスフラグ持ち越しを実装（2026-07-25）。実機Aタイプ準拠で揃うまで保持・無告知。リーチ目の前提
- [x] `main.ts` から重複ロジックを4クラスへ分離（2026-07-26・2338→2152行、テスト84→110件）
  - `StopController` 停止制御 / `BonusSession` ボーナス区間 / `RoundResolver` 判定と払い出し / `EffectEligibility` 演出の可否
  - 監査テストと出玉シミュレーターも同じクラスを通すよう統一。20万G×4段階の出玉が分離の前後で完全一致
- [x] BETから全停止、配当、ボーナス終了までを通す統合テストを追加（2026-07-27）
  - `tests/integration/spin-cycle.test.ts`。main.ts と同じ実クラス・同じ順序を
    node 上で再現する。これでビタ上乗せが小役で0枚になるバグを検出した
- [x] 回転中にAUTOをOFFにした時、現在ゲームを全停止まで消化してから終了（2026-07-27）
  - 消化中は「AUTO 消化中」（銀色）でONと区別。もう一度押せばAUTOへ復帰する
- [x] ランキングをルール版・速度・AUTO・ミッション条件で絞り込む（2026-07-27）
  - 既定を「公平に比べられる記録だけ」に寄せた（最新規則のみ・DEBUG除外がON）
  - 何件隠れているかを常に表示し、絞り込みで消えた記録を見落とさないようにした
- [x] ビルドごとに一意なIDを履歴へ記録（2026-07-27）
  - vite の define でコミットSHAを埋め込み `RunRecord.buildId` に保存。
    未コミットの変更があるビルドは `+dirty` を付けて手元ビルドと区別する
- [ ] `zikken/playground/mojislot-plan` の旧記述（章数、速度、配当値など）を現行実装と同期する

## P2: 演出と遊びやすさ

- [ ] 演出をJSONまたは登録型の定義へ移し、`main.ts` への直書きを減らす
- [ ] レバー、第1停止、第2停止、第3停止に連動する段階演出を追加する
- [ ] 遅れ、無音、先告知／後告知、バックライト、ブラックアウトを追加する
- [ ] ボーナスまたはCZで6通りの押し順ワードナビを検討する
- [ ] 低モーション、減光、色覚差対応、消音時の視覚通知を追加する
- [ ] クイズの学習履歴（問題別の遭遇・的中・苦手傾向）を保存する

## 比較実装メモ

- `../pachislot_tool` の `RoundExecutor`、`LotteryService`、`HybridRoleResolutionStrategy` は責務分離の参考にする
- 同リポジトリの候補停止・蹴りには役IDと図柄の混同や特定図柄のハードコードがあるため、そのまま移植しない
