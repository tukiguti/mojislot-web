# MOJISLOT TODO

2026-07-15 の実装・設計・関連リポジトリ調査で見つかった、即時修正の範囲を超える課題。
今後の制御・演出改善を以下で継続管理する。完了項目には実装時の決定を残す。

## P0: 仕様を決めてから着手

- [ ] **データカウンターの空欄問題を決める**（仕様書 30 §7）
  - 他の客がいないので、24台のうち今日データが載るのはせいぜい数台。カウンターは
    「どれを打つか選ぶ」道具としてほぼ機能していない
  - 設定差を演出率へ移した（下記）ことで合成確率の読み筋も弱くなったので決め時
  - 割り切る（現状）／前日までのデータを残す／他の客を模擬する、の3択
- [ ] 設定示唆の追加経路を決める（仕様書 30 §7）。現在はボーナス終了画面のみ
- [x] 設定差を**演出の出方**へ移す（2026-07-30・仕様書 30 §3）
  - 主役は無演出の割合（`NONE_MULTIPLIER` 1.100〜0.760）、ボーナス確率は従へ縮小
    （`BONUS_RATE_MULTIPLIER` 0.82〜1.24 → 0.93〜1.09）
  - このゲームの演出は**情報だけ**を持つ（ガセ無し・引き込み窓は演出で変わらない）。
    だから「無演出が減る → 何を狙えばよいか分かるゲームが増える → 取りこぼしが減る
    → 出玉が増える」が直接つながる。通常時に無演出を入れた時に神が196.2%→170%へ
    落ちた、あの効き方がそのまま設定差になる
  - **腕がある人ほど設定差が大きく出る**。中級 100.6→113.7%（13.1点・実機とほぼ同じ幅）、
    神 159.4→187.8%（28.4点）
  - 倍率の中心は加重平均が1.0になるよう置いた。低設定ほど出やすいので、単純に1.0を
    挟むとホール全体の水位が下がる
  - 演出の**種類の比**は設定で変えない。変えると「クイズが多い＝高設定」という別の
    読み筋が生まれる
  - 副作用: 突入率の設定差が 1/104〜1/96 まで縮み、**合成確率だけでは短時間で読めない**。
    主な手がかりは体感と終了画面の示唆に移る

- [x] 景品カウンターを場所の並びの**左端**へ移し、移動の動きを島と揃える（2026-07-30）
  - 実際のホールでも景品カウンターは入って手前の端にあり、島の列はその奥へ伸びる。
    `nav` 0 = カウンター、1..N = ISLANDS[nav-1]
  - **動きが違って見えていた**のは経路が2本あったため。島↔島は同じDOMを CSS の
    transform で滑らせるだけ（.42s cubic-bezier(.22,.7,.24,1)）だが、島↔カウンターは
    画面ごと作り直して拡大フェード（`hall-step-in` scale 1.05→1）を掛けていた
  - 画面ごと作り直す方も**同じ長さ・同じイージング**で横に滑らせる形に統一。
    歩いた向きに合わせて左右どちらから入るかを変える（`slide` 状態）
  - 寄り（会員カード・ランキング）から戻る時だけは拡大フェードのまま。あれは横に
    歩いたのではなく「近寄っていたのを引いた」動きなので、別で正しい
  - 隣が景品カウンターの時は通路の表示を「前の島」から「景品カウンター」に言い換える
- [x] 全ボタンが画面いっぱいの高さになっていたのを直す（2026-07-30）
  - 直前のCSS掃除で正規表現がルールを1つ食い違えていた。元は
    `button, .toggle-row, .zukan-row, .mission-row, .chapter-btn { touch-action... }`
    で、死んでいる `.toggle-row` から末尾までを消したため `button,` が孤立し、
    次の `body { min-height: 100vh }` とくっついていた
  - 結果、`<button>` すべてが `min-height:100vh`。会員カードの発行ボタン、券面の
    保存ボタン、ランキングの「島へ向かう」が画面いっぱいに伸びていた
  - 掃除をやり直した。正規表現をやめ、**波括弧を数えるパーサ**で「セレクタ群の
    全部が死んでいる時だけルールを消す・一部だけ死んでいるならその名前だけ外す」に。
    旧684ルール→575ルールで、**生きているセレクタの消失ゼロ・宣言の変更ゼロ**を
    新旧の突き合わせで確認した（コメント内の `{` も壊していたので併せて修復）
- [x] 島移動の導線を**通路の左右（◀▶）に統一**する（2026-07-30）
  - 島は左右の帯、カウンター前は足元のボタン、と場所ごとにバラバラだったのを、
    最初は「デスクトップは帯・スマホは足元」で揃えた。が、**幅で置き場所が変わる**
    設計だと幅の判定がずれた時に導線ごと入れ替わってしまい、実際そうなった
  - 分岐そのものを廃止し、幅によらず通路の帯に一本化。スマホのカウンター前は
    縦に流れるので帯を `position: fixed` にし、中身側に左右44pxの余白を取った
  - あわせて**幅の見張りが島の画面でしか回っていなかった**のを直した。
    `render()` が `phase === 'floor'` の時だけポーリングを開始していたので、
    カウンターや入口にいる間に幅が変わっても次の作り直しまで気付かなかった。
    見張りは常時、重い台カードの実測だけ `track` がある時に限る形へ
  - 足元バーの高さは `--hall-foot-h` に出したまま（トラックと帯の下端を揃えるため）
- [x] TOPページを廃止し、ホールの入口を起点にする（2026-07-30）
  - 遊ぶ・会員カード・ランキングの3導線はすべて入口に出ているので、手前にもう1枚
    メニューを置くと入場までに2回クリックさせることになる
  - `TopView.ts` と `#view-top` を削除。ルートは play/game/ranking/card の4つで、
    未知のhashとhash無しは `#/play`（＝入口）へフォールバックする
  - ゲーム内の `home-btn` は「ホールへ戻る」。入口の「← TOP」は消した
  - 削除した3画面（PlaySetup/CardView/RankingView）の CSS も併せて掃除。
    `.setup* .machine-* .toggle-* .ranking-* .rk-* .card-* .mc-* .landing*` の
    117ルールが死んでいた（style.css 4474→3794行）
  - `public/art/logo.webp` はどこからも参照されなくなった（入口のロゴは文字）。
    ブランド素材なので消さずに残してある
- [x] 景品カウンター（会員カード・ランキング）を実装する（2026-07-30）
  - カウンター前の引きの絵は `HallView` の1局面。**島の並びの隣にある場所**として置き、
    ← → で島から歩いて着く（`PLACES = 島数 + 1`）。場所インジケータにも細い金の点が増える
  - 寄りの2画面は `src/ui/CounterCard.ts` / `src/ui/CounterRanking.ts`（＋ `counter.css`）。
    `CardView.ts` / `RankingView.ts` は削除。ルート（#/card・#/ranking）はそのまま
  - 戻るは入口ではなく**カウンター前**へ返す。`mountHallView` が `showCounter(spot)` を返し、
    main-entry が戻り導線で呼ぶ
  - デザインの一次情報は claude.ai/design の「MOJISLOT 台を選ぶ.dc.html」。ただし
    **デザイン側のカード符号化・種データは使わない**。中身は既存の `CardManager` /
    `RunHistory` / `Member` に繋いである（保存形式もキーも実装側が正）
  - 券面の発行日のために `mojislot.memberSince.v1` を追加。**IDを作った瞬間にだけ**書く。
    後から埋めると実際と違う日付になるので、記録の無い会員は「—」のままにする
  - カウンター前のデータボードは本日の差枚上位3件（最新規則・DEBUG除外）。ダミーではない
  - 罠: `.ctr-msg{display:flex}` がブラウザ既定の `[hidden]{display:none}` に勝って、
    空のメッセージ欄が出ていた。`[hidden]` 側を明示して直した
  - 未確認: スマホ幅（760px未満）の実機表示。ホールと同じく分岐は書いたが目視できていない
- [x] 台選び画面をホール（入口→島→寄り）へ差し替える（2026-07-30）
  - デザインの一次情報は claude.ai/design の「MOJISLOT 台を選ぶ.dc.html」
  - `src/ui/HallView.ts`（画面）＋ `src/ui/hall.css`（構造）＋ `src/data/islandThemes.ts`（島ごとの筐体色）
  - 章カード5枚の一覧（`PlaySetup.ts`）を削除。選ぶのは章ではなく**24台のうち1台**
  - **設定（1〜6）とデータカウンターを台ごとへ繋ぎ替えた**。`settingFor(chapterId)` →
    `settingForMachine(machine)`、`recordSpin(chapterId)` → `recordSpin(machine.id)`。
    章で数えていたので同じ島の4台が同じ設定・同じデータになっていて、
    台を選び分ける意味が消えていた（`HallPolicy` に台版が書いてあったが未接続だった）
  - 選んだ台は `mojislot.machine.v1`。章は台から引くので両者がずれない
  - `MachineData` はキーの意味が変わったので v2 へ。日替わりで捨てるデータなので移行なし
  - スランプグラフを実データにした。デザインは台番をシードにした疑似乱数だったが、
    それでは「見た目は正常なまま嘘をつく」。差枚の推移を25ゲームごとに記録し、
    26点で詰まったら1つ置きに間引いて間隔を倍にする（常に今日の全体が入る）
  - プレイ設定の4トグルは**寄り画面の座る直前**へ。打つ直前が最後に確認する場所
  - リミックス島は章のステージ切替が未実装なので「調整中」。着席を塞いである。
    小役は各章から1つずつ取って混ぜた姿を見せる（4/6/8/10 の枚数の階段は保たれる）
  - 未確認: スマホ幅（760px未満）の実機表示。マークアップ分岐は書いたが目視できていない

- [x] レバー時の内部役抽選を導入し、演出抽選と当選結果を分離する（2026-07-16）
  - `miss / replay / core / cherry / reg / big` などの内部役を `RoundContext` に保持する
  - 各章の具体役ごとに`internalRoleRate`を持たせ、種別やリール文字枚数を介さず直接抽選する
  - 「ガセ演出なし」を維持し、当選役を表現できる演出だけをレバーON後に抽選する
  - 表示役は`RoleResolver`で内部役と一致するラインだけを成立扱いにする
  - 初期レートは暫定値。再現可能シミュレーター完成後に再調整する
- [x] レバー時の内部役抽選を1枚役へ拡張（2026-07-24）
  - 表示役と内部役を分離（表示役8＋1枚役 → 内部役10）
- [x] 押し順役・押し順ナビを廃止（2026-07-25）
  - 押し順は「役の種類」ではなく**停止制御の入力**という設計へ戻した。順押しが前提
  - 内部役 13種 → 10種。だんご・みかんは普通の小役になり、示唆/クイズ/狙えにも登場する
- [x] 1枚役を実役化（2026-07-25）
  - 「2個テンパイ＝1枚」の擬似実装を廃し、各章2〜3種の**実在の並び**（既存文字の
    「単語にならない」3文字・`singleYaku`）を定義。判定・払い出し・蹴りの一般レイヤに載せた
  - 選定は「miss時に蹴りで必ず回避できる（②漏れ0）」を制約に引き込みカバー率を最大化
    （food なしん/みしご/もしん 80% ・ verb 81% ・ animal 76% ・ security 90% ・ yasai 80%）
  - 当選時は自動引き込み（中段・窓4）＝実機の「当選小役は勝手に揃う」。窓外はこぼれ
  - コンボ・図鑑・成立演出には乗せない（こぼし扱い）。フラグは複数ID（exceptYakuIds）へ一般化
  - `replay` kind は廃止（再遊技は実装しない決定）。とうふ等は core へ整理
- [x] 停止制御を決定的なテーブル制御へ移行（2026-07-24）
  - 当選役以外を4コマ以内で必ず回避。確率・演出限定・カテゴリ限定を撤廃
  - リール配列を構築的に再設計し②（揃っているのに0枚）を全5章ゼロ化（tests/audit）
- [x] 本番ロジックを利用する再現可能な出玉シミュレーターを作る（2026-07-24）
  - `tests/sim/payout-sim.test.ts`（`SIM=1` で実行）。腕別の機械割を再測定しレート再調整済み
- [x] ビタ押しを出玉へ反映する（2026-07-27）
  - 判定を「**引き込みなし＝自力停止**」へ変更。役に必要なリールを1本残らず
    自力で止めた時だけ配当に上乗せする（`payout.bitaMultiplier`）
  - 従来の ±12ms は出目に一切影響しない指標だった（どちらにせよ最寄りセルへ
    スナップされる）ので、押し味のSE・波紋にだけ残しスコアからは外した
  - 貢献リールを役の文字数で数えるよう修正。チェリー（2文字役）で右リールの
    自力停止まで要求してしまっていた
  - 到達率（実測）: 初心者4.1% / 中級10.0% / 上級27.1% / 神69.6%
- [x] フリーズを「強レア役」化する（2026-07-26）
  - 独立抽選をやめ、内部役テーブルに `freeze: true` のBIG専用役（各章1/2000・ボーナス中0）を追加。
    確率の一次情報は `data/yaku/` へ一本化し、`tuning.freeze` は `spinSpeed` だけになった
  - `forYaku` はフリーズ役を選ばない＝確定告知ランプ・持ち越しの消化で暴発しない
  - 出玉シミュレーションが 80.1/103.5/130.1/170.3% → 81.7/105.6/132.5/173.4% に変化したが、
    これは**測定の修正**。従来フリーズはテーブル外だったためシミュレーターが数えておらず、
    実際の機械割は前後でほぼ不変（差は1e-6オーダー）

## P0: 仕様を決めてから着手（続き）

- [x] 台選びをホールの島形式にし、設定（1〜6）と設定推測を導入（2026-07-27）
  - 6島×4台＝24台。同じ島はリール配列が同じで違うのは設定だけ＝覚えた配列が島の中で活きる
  - 設定は日付＋台番号から決定的に決まる（打っている間は変わらない／台ごとに違う／日替わり）
  - 小役では差が付かない（数百ゲームで見抜けると推測が成立しない）
  - ホール方針（末尾／島／角／全台系／通常）→ 各台の設定 → ポスターの順で決める
  - 設定示唆はボーナス終了画面。「出たら意味がある／出なくても否定にならない」
  - 詳細: zikken/playground/mojislot-plan/30_hall-and-settings.md
- [x] 役・島構成を作り直す（2026-07-27）
  - 役名とリールに出る文字が食い違う（ナマズ／なまず、ドラゴン→ドラゴ）、ジャンルから
    浮いた役（ポテチは野菜でない、メモリ・コールはセキュリティ用語でない）を一掃した
  - REGはリールに自分の文字を持たない（BIG1の頭2文字＋別の役の右）。この制約から
    **BIG2つとREGの3語が同時に成立する組**を探す形に変えた。「寿司酢」は文字の余りに
    名前を付けたものだった
  - カタカナの野菜名は2文字目で分岐しないため野菜だけではREGを作れず、果物まで広げて
    ライチ／ライム／プラムに。島名を**カタカナ八百屋**へ（内部IDは `yasai` のまま）
  - ②はリール配列の焼きなましで再度ゼロ化。探索ツールを `tests/tools/optimize-reels.test.ts`
    としてリポジトリに残した（前回はスクリプトが失われ、手法から書き直しになった）
  - 1枚役も文字が変わるので組み直した。②を抑えようとコマ数を絞ると引き込みが届かなくなる
    ので、3文字のコマ数の積が36〜48に収まる語を選び成立率43〜55%に揃えた（一度10%まで
    落として出玉を下げた）。増えた②は焼きなましで処理する、という順序に落ち着いた
  - 出玉は 初心者89.1% / 中級110.2% / 上級140.1% / 神196.2%（作り直し前より3〜6ポイント高い。
    ボーナスの狙いやすさが上がったため）
  - 詳細: zikken/playground/mojislot-plan/28_yaku-chapter-redesign.md
- [x] 役の到達性監査を追加（2026-07-27）
  - `tests/audit/yaku-reachability.test.ts`。②の監査は「当選していない役が出ないか」しか
    見ておらず、**当選しているのに絶対に揃わない**を検出できなかった
  - 実際に八百屋のREGが成立率0%になり、永久持ち越し→無告知→島が丸ごと機能停止していた。
    全テストが通ったまま壊れ、出玉シミュレーションの数字で初めて気づいた
- [x] カットインを役色から手続き生成する（2026-07-28）
  - 一枚絵を**任意**にした。役データに `cutinArt` を書いた役だけが絵を使い、書かない役は
    役色から組み立てる（放射グロー・光線・縁が役色、背景に役名を輪郭で沈める）
  - 以前は「主役＝島の一枚絵、バー役＝`_bar` 絵」と役の位置で暗黙に決めていたため、役を
    差し替えると絵だけ前の役のまま残った。役が自分の絵を名指しする形にして構造的に解消
  - 副産物として BIG1=金 / BIG2=青 / REG=銀 と色が分かれた（以前はBIG2も金だった）
- [ ] カットインの一枚絵を用意するか決める（**画像生成が要る**）
  - 今は全島とも生成側で出ている。絵を足すなら役へ `cutinArt` を1行書く
  - 動詞島の2枚（いきる＝命の樹／やすむ＝三日月）は役と一致しているのですぐ使える
  - 対応表: zikken/playground/mojislot-plan/16_art-assets-and-lcd-layout.md
- [ ] 台選び画面をホール島形式へ差し替える（**デザイン待ち**）
  - 裏側（24台・方針・ポスター・設定・データ・示唆）は実装済み。画面だけ未配線
  - 受け渡し資料: zikken/playground/mojislot-plan/29_design-handoff.md
- [x] リール図柄は色タイル＋文字を正式とする（2026-07-28・判断のみ、コード変更なし）
  - 画像トグルは既定OFFなので普段誰も見ていない。ビタ押しが出玉に直結する今、図柄の
    識別しやすさは難易度そのもので、文字が読みやすい方が競技性に合う
  - 「絵と文字の融合（カリグラム）」案は見分けに一拍かかる＝難易度を上げる方向だったので見送り
  - `noArt` の仕組みで絵を用意した役だけ絵になる。決めたのは「今は作らない」だけ
- [x] 通常時に無演出（none）を入れる（2026-07-28）
  - `rollAvailable` は「役を表現できる演出」だけから抽選しており候補に none が無かった。
    そのため `effectRates.default.none = 0.5` はどこからも読まれず、当たれば必ず狙える状態
    だった。none を含む `roll()` も呼び出し元が無かったので削除した
  - 候補に none を混ぜた。ボーナス中は `rates.bonus.none = 0.0` なので重み0で落ちる
  - 演出が出るのは全ゲームの約28%（表示役を持つ内部役57% × 50%）。残りは何を狙うか
    分からないまま押すが、引き込みは当選役に効いたままなので当たりは拾える
- [x] 配当を組み替える（ボーナスの価値を「区間」へ寄せる／2026-07-28）
  - 小役 5枚一律 → **4 / 6 / 8 / 10**。枚数から役を逆算できるようにする（実機のベル・スイカ
    と同じで枚数が役の名札になる）。いちばん出やすい役をいちばん低配当に置いた
  - カテゴリ別の `baseMultiplier` では小役4種を区別できないので、**役ごとの `payout`** を
    追加した。リミックス島も「位置と枚数の対応」で覚える設計なので、枚数は役と同じ場所に置く
  - `tests/data/payout-tiers.test.ts` が全5島で階段の一致と「出やすい役ほど安い」を見張る
  - REG 14 → **10**、BIG 34 → **15**。恩恵は揃えた瞬間ではなくボーナス中の100%演出区間
- [x] P2の演出設計（2026-07-29）
  - 演出は「**告知**（役をどこまで明かすか・出玉が動く）／**予兆**（気配だけ・中立）／
    **タイミング**（押す前か後か）」の3種類に分かれる。一列に並べていたのが整理不足だった
  - 示唆を5色へ分割。旧構成は青が小役4種＋チェリーの**5択**で、当てずっぽうは腕で埋められず、
    目押しが本体のこのゲームで示唆だけが運の演出になっていた（取りこぼしが狙え6.8%に対し37.4%）
  - 指定を**カテゴリから役の枠（位置）へ**。tuning は全島共有なので島ごとの役IDは書けないが、
    全島とも構造が同じなので core0〜core3 / cherry / reg / big0 / big1 で指せる
  - 発展条件を「中段に来たら」→「可視3コマのどこかに来たら」へ（72%→87%）
  - 予兆系（無音・バックライト・ブラックアウト）は**強レア役とボーナス専用に取っておく**方針。
    無演出の穴埋めに使うと、いざ強い場面で使う手札が無くなる
  - 残: 示唆の候補数を絞る強弱（同じ色で2択/3択を出し分ける）
- [x] ボーナス中のおかわり率を3倍にする（2026-07-29）
  - BIG 1/1981＝BIG18G中に0.9%＝**平均110回のBIGに1回**しか見られず、機能として
    存在していないのと同じだった。REG 1/154・BIG 1/660 で7〜8回のBIGに1回
  - 出玉への寄与は小さい（神+3.6点）。おかわり自体がレアなので総量に効かない
- [ ] リミックス島のステージ切替を実装する（台の枠だけある状態）
  - **リール配列は共通、文字だけステージごとに変わる**。配列まで変わると覚え直しのコストが
    高すぎてステージ切替が苦行になる
  - **位置ごとの枚数をステージ間で共通にする**（位置3は常に6枚）。プレイヤーが覚えるのは
    「6枚の役はこの位置」という位置と枚数の対応になり、ステージが変わっても無効にならない
  - 制約: ②＝0 を5ステージ全部で満たす共通パターンが要る。焼きなましの目的関数を
    「5ステージの②の合計」に変えて探す。解が無ければステージごとに一部だけ差分を持たせる
  - 却下: 全ステージの文字を1本のリールに載せる案（35文字は21コマに入らない／ひらがなと
    カタカナの混在は識別の負荷が理不尽に寄る）
- [x] 出玉レートを再調整する（2026-07-28）
  - **初心者87.6% / 中級105.2% / 上級130.0% / 神176.0%**（変更前 89.1 / 110.2 / 140.1 / 196.2%）。
    設定1〜6で中級 101.3〜110.0%、突入 1/115〜1/93
  - 無演出を入れた時点で全腕が25点前後落ちて初心者64.6%になった。**水位は小役の枚数が
    ほぼ全部を決める**（1枚上げると全腕が10〜12点動く）ので、階段の間隔はそのまま2枚上げた。
    1枚刻みは微調整には粗すぎるので、数点を合わせる時はボーナス倍率の小数を使う
  - 無演出を薄くしても水位は戻らない（0.5→0.2 で初心者73.3%止まり、代わりに腕の開きが拡大）。
    狙える機会を増やすのは上手い人にだけ効くので、水位の調整弁にはならない
  - **腕の開きを詰められるのはコンボ天井だけ**。4.5→3.0 で神 −10点・初心者 −1点。
    2.5まで下げると開きはさらに詰まるが、ボーナス倍率2.2との差が0.3しか残らず消化中に
    コンボの伸びを感じられなくなるので3.0で止めた
  - 残る開きはボーナス中の目押し精度そのもの（純増 6.0枚 対 17.4枚）。ボーナス中は
    無演出が出ず狙う役は全員分かっているので、これ以上詰めるなら引き込みコマ数を
    増やすしかなく、それは目押しの価値を削る方向になる
  - 詳細: zikken/playground/mojislot-plan/31_effect-and-payout-plan.md §7
- [x] ボーナス中の1枚役をこぼしにする（2026-07-28）
  - ボーナス中も1枚役が5.7%で抽選されていた。表示役を持たないので演出が出ず、固定1枚で
    倍率も乗らず、しかも連チャンを切る。ボーナス中はハズレが0なので、上手い人にとって
    連を切る唯一の要因がこれだった。**BIGあたり11〜30枚**が消えていた
  - ボーナス中の抽選を0にして小役・チェリーへ比例配分（おかわり頻度は動かさないため
    REG/BIGには配らない）。代わりにボーナス中だけ許可リストへ1枚役を足し、当選役を
    引き込めなかった時の受け皿にした。通常時は従来どおり抽選で降ってくる役のまま
  - **引き込みの横取りに注意**。1枚役を許可リストへ入れると、図柄がたまたま近いという
    理由で本来取れる小役を捨てて1枚に落とす。カテゴリ優先だけでは足りず、当選役が
    引き込み圏外の時に蹴りで斜めに揃っていた目を潰していた（1章で59件）
  - 1枚役は引き込み対象から外し、**当選役が届かない最終停止でだけ**拾いに行く構造にした。
    あわせて pullIn の返り値を number|null にして「そのまま揃う(0)」と「届かない」を区別
  - `tests/audit/bonus-spill.test.ts` が全押下位置(21^3)で通常時とボーナス中を回し、
    通常時に揃う局面がボーナス中も必ず揃うことを見張る
  - こぼし率（ボーナス中に外したゲームのうち1枚役へ落ちた割合）: 初心者8.6% / 神5.1%
  - 上がった分はボーナス倍率 ×2.2→×1.95 で戻した。副作用でコンボの伸び幅が
    +36%→+54% に広がり、消化中にコンボが効く感触はむしろ強くなった
- [x] 島名から文字種を外す（2026-07-29）
  - ひらがな寿司→寿司 / カタカナ動物→動物 / ひらがな動詞→動詞 / カタカナ八百屋→八百屋
  - 島名はホールの島看板と筐体のタイトルパネルに出る。デザイン仕様は短い名前で作られていて、
    実装の `chapters.ts` だけ文字種付きだった。デザインを取り込む時に迷うので実装側を寄せた
  - 文字種の情報は台選びカードの `description` に並ぶ役名で分かるので失われない
  - `id` は据え置き（寿司が `hiragana_food`、八百屋が `yasai`）。既存の実戦履歴と図鑑は
    chapterId で引くので影響なし
- [x] ひらがな食べ物島をひらがな寿司島へ作り直す（2026-07-29）
  - REGは「7の左2文字＋バーの最後1文字」で作る規則だが、食べ物島だけ外れていた
    （7=いなり・バー=まぐろ なら いなろ になるが語にならず、小役だんごの「ご」を借りていた）
  - 規則を満たすには先頭2文字を共有する3文字語のペアが要る。これが**魚介に集中している**ため、
    島ごと寿司ネタで統一した。しゃけ/たらこ/しゃこ＋いわし・さんま・かつお・まぐろ＋えび
  - リール配列を焼きなまし（②40→0）、停止テーブル・リーチ目を再生成、クイズ21問を書き直し
  - `tests/tools/pick-single-yaku.test.ts` を追加。1枚役は積の経験則では足りず、
    到達率と②のトレードオフを実測して選ぶ（この島は1529組目で②＝0に到達）
  - 統合テストが役IDを直書きしていたので coreYaku[0] 参照へ変更
- [x] 引き込みを中段固定からライン単位へ（2026-07-29）
  - 第1・第2停止が中段だけを狙っており、上段・下段・斜めの3ラインが設計上ほぼ死んでいた
  - 役×ペイラインごとに、そのラインが要求する行へ寄せる形へ。第1停止は停止テーブルから
    返るので computeFirstStopSlip も同様に直し、全章の表を焼き直した
  - **中段が第一候補で上下段は逃げ道**。全ラインを等価にすると中段が特別な位置でなくなり、
    示唆の発展68%→38%・中段告知46.5%→14.1%と告知の仕組みが道連れになる
  - 引き込み窓が実質5→7コマ。精度の低い人ほど得をするので腕の開きが1.98倍→1.82倍
  - 上がった分はボーナス倍率 ×1.95→×1.4 で戻した。コンボの伸び幅は +54%→+114% に拡大
  - 実測 91.2 / 104.4 / 124.1 / 166.1%
- [x] 1確の誤告知監査を追加（2026-07-29）
  - 「非ボーナスフラグでは専用図柄を第1停止の中段に置かない」は停止テーブルで
    担保しているが**保証ではなく best-effort**（窓内が全滅すれば諦める）。実測0件だが
    リール配列を焼きなまし直せば静かに壊れ得るので回帰ガードを置いた
  - `tests/audit/first-stop-tell.test.ts`。停止テーブルを1箇所壊して落ちることも確認済み
- [x] 告知の文字を全部消した（2026-07-29）
  - 1確・リーチ目・チェリー重複。実機でも文字は出ないし、出目を読ませる遊びを文字で潰す
- [x] チェリー重複の点灯をBETで前倒し（2026-07-29）
  - 900msのタイマー任せだったので、すぐBETすると次ゲームの回転中に点いていた。
    そのゲームの内部役はボーナスではないので「点いたのに揃わない」うえ1ゲーム損する
- [ ] チェリー重複の名前を実装に合わせるか、実装を実機へ寄せるか決める
  - 実機の重複当選は内部抽選の時点で同時当選する。このゲームは**成立後の後付け抽選**で、
    こぼすと権利ごと消える。体験は近いが機構は別物
- [ ] 小役の枚数を図鑑に表示するか決める
  - 枚数を役の名札にした以上、打って覚える以外に確認手段が無い状態でよいか
  - 出すなら `ZukanOverlay` の行に1列足すだけ。ビタ押し前提のゲームで「配当表を見れば
    分かる」のが親切か、覚えること自体を遊びに残すかの判断

## P1: 制御と保守性

- [x] 予告・確定告知の8コマ強アシストを廃止し、実機準拠の一律4コマへ（2026-07-25）
- [x] ボーナスフラグ持ち越しを実装（2026-07-25）。実機Aタイプ準拠で揃うまで保持・無告知。リーチ目の前提
- [x] `main.ts` から重複ロジックを4クラスへ分離（2026-07-26・2338→2152行、テスト84→110件）
  - `StopController` 停止制御 / `BonusSession` ボーナス区間 / `RoundResolver` 判定と払い出し / `EffectEligibility` 演出の可否
  - 監査テストと出玉シミュレーターも同じクラスを通すよう統一。20万G×4段階の出玉が分離の前後で完全一致
- [x] BETから全停止、配当、ボーナス終了までを通す統合テストを追加（2026-07-27）
  - `tests/integration/spin-cycle.test.ts`。main.ts と同じ実クラス・同じ順序を
    node 上で再現する。これでビタ上乗せが小役で0枚になるバグを検出した
- [x] 回転中にAUTOをOFFにした時、現在ゲームを全停止まで消化してから終了（2026-07-27）
  - 消化中は「AUTO 消化中」（銀色）でONと区別。もう一度押せばAUTOへ復帰する
- [x] ランキングをルール版・速度・AUTO・ミッション条件で絞り込む（2026-07-27）
  - 既定を「公平に比べられる記録だけ」に寄せた（最新規則のみ・DEBUG除外がON）
  - 何件隠れているかを常に表示し、絞り込みで消えた記録を見落とさないようにした
- [x] ビルドごとに一意なIDを履歴へ記録（2026-07-27）
  - vite の define でコミットSHAを埋め込み `RunRecord.buildId` に保存。
    未コミットの変更があるビルドは `+dirty` を付けて手元ビルドと区別する
- [ ] `zikken/playground/mojislot-plan` の旧記述（章数、速度、配当値など）を現行実装と同期する

## P2: 演出と遊びやすさ

- [ ] 示唆を4色へ分割し、発展条件を緩和する（設計中・32章）
  - 青＝安い小役2つ / 緑＝高い小役2つ＋チェリー / 赤＝REG / 金＝BIG。
    今は青が小役4種＋チェリーの5択で、当てずっぽうが腕で埋められない
  - 発展条件を「中段に来たら」→「可視3コマのどこかに来たら」。発展率 69%→82%
  - 示唆の候補数を絞る強弱（5択→3択→2択）も別軸として検討する
- [ ] 遅れを追加する（設計中・32章）
  - レバーONからリール始動まで0.3秒ほど間を置く。**ハズレでは出さない**（嘘なし）
  - 出現率自体は低く、出た時の内訳はボーナスへ寄せる＝「ボーナス濃厚だが確定ではない」
  - ボーナスが狙えるようになるので出玉が上がる。確率は測ってから決める
- [ ] 演出をJSONまたは登録型の定義へ移し、`main.ts` への直書きを減らす
- [ ] レバー、第1停止、第2停止、第3停止に連動する段階演出を追加する
- [ ] 無音・バックライト・ブラックアウトなど予兆系を追加する
  - **強レア役とボーナス専用に取っておく**方針（2026-07-29決定）。無演出の穴埋めには使わない
- [ ] 先告知／後告知の使い分けを決める（現行は確定告知ランプ＝先告知のみ）
- [ ] ボーナスまたはCZで6通りの押し順ワードナビを検討する
- [ ] 低モーション、減光、色覚差対応、消音時の視覚通知を追加する
- [ ] クイズの学習履歴（問題別の遭遇・的中・苦手傾向）を保存する

## P2: 音

現状は `SfxEngine` / `BgmEngine` とも **Web Audio のオシレータでその場合成**しており、
外部音源ファイルを1つも持たない。矩形波のビープとCメジャーの短いループで、
「鳴っている」以上のことはしていない。演出の密度を上げるほど音の薄さが目立つ。

- [ ] 効果音を作り込む。今は全部 `beep()` の合成音で、レバー・停止・当たりの区別が音色の差だけ。
      停止音は3リールとも同じで、第3停止の重みが音に出ていない
- [ ] BGMを作り込む。通常・ボーナスの2曲だけで、どちらも8小節の単純ループ。
      ボーナス中の連チャンや区間の伸びが音に反映されない
- [ ] 音源ファイルを使うか合成のままかを決める。合成は容量ゼロ・ライセンス問題なしだが表現の上限が低い。
      ファイルにするなら配布サイズと権利の確認が要る
- [ ] 音量バランスを取る（現状 master 0.25 固定。SEとBGMの相対音量を調整していない）
- [ ] 消音時に音の情報が失われる。視覚での代替（P2の「消音時の視覚通知」）と対で考える

## 比較実装メモ

- `../pachislot_tool` の `RoundExecutor`、`LotteryService`、`HybridRoleResolutionStrategy` は責務分離の参考にする
- 同リポジトリの候補停止・蹴りには役IDと図柄の混同や特定図柄のハードコードがあるため、そのまま移植しない
